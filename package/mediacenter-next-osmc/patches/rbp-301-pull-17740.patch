From 687c174c4793a86904f3cde77451406127b799ed Mon Sep 17 00:00:00 2001
From: djp952 <djp952@gmail.com>
Date: Thu, 23 Apr 2020 10:36:01 -0400
Subject: [PATCH] [PVR] Use active channel name when 'Importing guide from
 clients'

---
 xbmc/pvr/epg/EpgContainer.cpp | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/xbmc/pvr/epg/EpgContainer.cpp b/xbmc/pvr/epg/EpgContainer.cpp
index 869e2e693976..c1bc64a78bdc 100644
--- a/xbmc/pvr/epg/EpgContainer.cpp
+++ b/xbmc/pvr/epg/EpgContainer.cpp
@@ -663,7 +663,8 @@ bool CPVREpgContainer::UpdateEPG(bool bOnlyPending /* = false */)
       continue;
 
     if (bShowProgress && !bOnlyPending)
-      progressHandler->UpdateProgress(epg->Name(), ++iCounter, m_epgIdToEpgMap.size());
+      progressHandler->UpdateProgress(epg->GetChannelData()->ChannelName(), ++iCounter,
+                                      m_epgIdToEpgMap.size());
 
     if ((!bOnlyPending || epg->UpdatePending()) &&
         epg->Update(start,
