From 6a3375d2372ea8adb8e47f562e6d13fd080b39ff Mon Sep 17 00:00:00 2001
From: phunkyfish <phunkyfish@gmail.com>
Date: Sun, 3 May 2020 12:12:13 +0100
Subject: [PATCH 1/2] [filesystem] switch INFO log statements to DEBUG

---
 xbmc/filesystem/DllLibCurl.cpp | 4 ++--
 xbmc/filesystem/FileCache.cpp  | 3 ++-
 2 files changed, 4 insertions(+), 3 deletions(-)

diff --git a/xbmc/filesystem/DllLibCurl.cpp b/xbmc/filesystem/DllLibCurl.cpp
index 9079a0ecce19..f10ea25b0357 100644
--- a/xbmc/filesystem/DllLibCurl.cpp
+++ b/xbmc/filesystem/DllLibCurl.cpp
@@ -149,7 +149,7 @@ void DllLibCurlGlobal::CheckIdle()
   {
     if (!it->m_busy && (XbmcThreads::SystemClockMillis() - it->m_idletimestamp) > idletime)
     {
-      CLog::Log(LOGINFO, "%s - Closing session to %s://%s (easy=%p, multi=%p)", __FUNCTION__,
+      CLog::Log(LOGDEBUG, "%s - Closing session to %s://%s (easy=%p, multi=%p)", __FUNCTION__,
                 it->m_protocol.c_str(), it->m_hostname.c_str(), static_cast<void*>(it->m_easy),
                 static_cast<void*>(it->m_multi));
 
@@ -225,7 +225,7 @@ void DllLibCurlGlobal::easy_acquire(const char* protocol,
 
   m_sessions.push_back(session);
 
-  CLog::Log(LOGINFO, "%s - Created session to %s://%s", __FUNCTION__, protocol, hostname);
+  CLog::Log(LOGDEBUG, "%s - Created session to %s://%s", __FUNCTION__, protocol, hostname);
 }
 
 void DllLibCurlGlobal::easy_release(CURL_HANDLE** easy_handle, CURLM** multi_handle)
diff --git a/xbmc/filesystem/FileCache.cpp b/xbmc/filesystem/FileCache.cpp
index a98f6956cf78..6a0ae26d2197 100644
--- a/xbmc/filesystem/FileCache.cpp
+++ b/xbmc/filesystem/FileCache.cpp
@@ -344,7 +344,8 @@ void CFileCache::Process()
       }
       else
       {
-        CLog::Log(LOGINFO, "CFileCache::Process - Source read didn't return any data! Hit eof(?)");
+        CLog::Log(LOGDEBUG, 
+                  "CFileCache::Process - Source read didn't return any data! Hit eof(?)");
 
         m_pCache->EndOfInput();
 

From 7a0ade6b8dd2bf90759d7ac4172efc57700f1be7 Mon Sep 17 00:00:00 2001
From: phunkyfish <phunkyfish@gmail.com>
Date: Mon, 4 May 2020 14:59:13 +0100
Subject: [PATCH 2/2] [pvr] switch INFO log statements to DEBUG

---
 xbmc/pvr/channels/PVRChannelGroup.cpp | 8 ++++----
 xbmc/pvr/epg/EpgContainer.cpp         | 2 +-
 xbmc/pvr/epg/EpgTagsContainer.cpp     | 2 +-
 3 files changed, 6 insertions(+), 6 deletions(-)

diff --git a/xbmc/pvr/channels/PVRChannelGroup.cpp b/xbmc/pvr/channels/PVRChannelGroup.cpp
index 2c12168a2d37..40333b066a9a 100644
--- a/xbmc/pvr/channels/PVRChannelGroup.cpp
+++ b/xbmc/pvr/channels/PVRChannelGroup.cpp
@@ -498,8 +498,8 @@ bool CPVRChannelGroup::AddAndUpdateChannels(const CPVRChannelGroup& channels, bo
                  bUseBackendChannelNumbers, newMember->clientChannelNumber);
 
       bReturn = true;
-      CLog::Log(LOGINFO, "Added %s channel '%s' to group '%s'",
-                IsRadio() ? "radio" : "TV", existingAllChannelsMember->channel->ChannelName().c_str(), GroupName().c_str());
+      CLog::Log(LOGDEBUG, "Added %s channel '%s' to group '%s'", IsRadio() ? "radio" : "TV",
+                existingAllChannelsMember->channel->ChannelName().c_str(), GroupName().c_str());
     }
     else
     {
@@ -516,8 +516,8 @@ bool CPVRChannelGroup::AddAndUpdateChannels(const CPVRChannelGroup& channels, bo
         bReturn = true;
       }
 
-      CLog::Log(LOGINFO, "Updated %s channel '%s' in group '%s'",
-                IsRadio() ? "radio" : "TV", existingMember->channel->ChannelName().c_str(), GroupName().c_str());
+      CLog::Log(LOGDEBUG, "Updated %s channel '%s' in group '%s'", IsRadio() ? "radio" : "TV",
+                existingMember->channel->ChannelName().c_str(), GroupName().c_str());
     }
   }
 
diff --git a/xbmc/pvr/epg/EpgContainer.cpp b/xbmc/pvr/epg/EpgContainer.cpp
index 2049c2d3926f..fbd921356f78 100644
--- a/xbmc/pvr/epg/EpgContainer.cpp
+++ b/xbmc/pvr/epg/EpgContainer.cpp
@@ -282,7 +282,7 @@ bool CPVREpgContainer::PersistAll(unsigned int iMaxTimeslice) const
   {
     if (epg.second && epg.second->NeedsSave())
     {
-      CLog::Log(LOGINFO, "EPG Container: Persisting events for channel '%s'...",
+      CLog::Log(LOGDEBUG, "EPG Container: Persisting events for channel '%s'...",
                 epg.second->GetChannelData()->ChannelName().c_str());
 
       bReturn &= epg.second->Persist(database);
diff --git a/xbmc/pvr/epg/EpgTagsContainer.cpp b/xbmc/pvr/epg/EpgTagsContainer.cpp
index 21f4b11c1699..fd607938cebe 100644
--- a/xbmc/pvr/epg/EpgTagsContainer.cpp
+++ b/xbmc/pvr/epg/EpgTagsContainer.cpp
@@ -583,7 +583,7 @@ void CPVREpgTagsContainer::Persist(bool bCommit)
   {
     m_database->Lock();
 
-    CLog::Log(LOGINFO, "EPG Tags Container: Updating %d, deleting %d events...",
+    CLog::Log(LOGDEBUG, "EPG Tags Container: Updating %d, deleting %d events...",
               m_changedTags.size(), m_deletedTags.size());
 
     for (const auto& tag : m_deletedTags)
